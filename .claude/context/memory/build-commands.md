# Build Commands

## Development

- Start dev server: `npm run dev` (Runs `next dev`)

## Build

- Production build: `npm run build` (Runs `next build`)

## Start

- Start production server: `npm run start` (Runs `next start`)

## Linting

- Run ESLint: `npm run lint` (Runs `eslint`)
